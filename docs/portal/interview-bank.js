/* Interview Bank data: 50 technical Q&A + 50 architecture decisions, grounded in the
 * actual implementation in this repository. Loaded before app.js. */

const INTERVIEW_QUESTIONS = [
  { category: "crypto", question: "What's the difference between hashing, encryption, and signing?", answer: "Hashing (SHA-256) produces a fixed-size fingerprint used to detect tampering, but it is one-way and doesn't use a secret. Encryption (AES-256-CBC+HMAC here) transforms data so it's unreadable without a secret key, giving confidentiality. Signing (EC P-256 over a hash) proves authenticity and integrity with a private key, verifiable by anyone holding the public key/certificate, without hiding the content." },
  { category: "crypto", question: "Why EC P-256 instead of RSA for signing in this repo?", answer: "EC P-256 gives roughly RSA-3072-equivalent security with much smaller keys and signatures, faster generation, and is well supported by the openssl CLI used throughout. The repo's security notes require EC P-256 or RSA-3072+ as the minimum, with P-256 chosen as the default for smaller envelopes and faster CI." },
  { category: "crypto", question: "Why SHA-256 and not SHA-1/MD5?", answer: "SHA-1 and MD5 both have practical collision attacks and are explicitly banned in this repo's security notes. SHA-256 has no known practical collision/preimage attack and is hardcoded as the envelope's digest_algorithm." },
  { category: "crypto", question: "What is HMAC and why is it used here?", answer: "HMAC is a keyed hash proving both integrity and authenticity using a shared secret. encrypt_artifact.py uses it as the MAC in an Encrypt-then-MAC construction, so ciphertext tampering is caught (via hmac.compare_digest, constant-time) before any AES decryption is attempted." },
  { category: "crypto", question: "Why wasn't AES-GCM used for confidentiality?", answer: "Testing showed OpenSSL's enc CLI rejects AEAD ciphers outright ('enc: AEAD ciphers not supported'), and the design goal was zero extra Python crypto dependencies. AES-256-CBC + HMAC-SHA256 (Encrypt-then-MAC) gives equivalent authenticated-encryption security using only CLI primitives that actually work." },
  { category: "crypto", question: "What does 'Encrypt-then-MAC' mean and why does the order matter?", answer: "It means encrypting first, then computing the MAC over the ciphertext (IV||ciphertext), not the other way around. This lets the verifier reject tampered/forged ciphertext before ever running decryption on attacker-controlled data, closing off padding-oracle-style attacks that MAC-then-encrypt is prone to." },
  { category: "signing", question: "What's inside the 'secure-signing-envelope/v1' JSON format?", answer: "format, method (openssl|pki), digest_algorithm (sha256), artifact_sha256, signature_base64, and for pki mode also certificate_pem and chain_pem \u2014 a versioned, self-describing detached signature so the artifact bytes are never modified." },
  { category: "signing", question: "Why detached signatures instead of embedding the signature inside the binary?", answer: "Detached .sig envelopes keep the artifact byte-identical to its unsigned build, work uniformly across ELF binaries, kernel modules, and non-executable data, and avoid needing a container format every build target must support." },
  { category: "signing", question: "How does CMake integration sign can-utils binaries without modifying the vendored submodule?", answer: "A parent CMakeLists.txt add_subdirectory()s the untouched submodule, and signing/cmake/Signing.cmake's sign_target() creates its own add_custom_command(OUTPUT ...) depending on the vendor target via $<TARGET_FILE:target> \u2014 required after discovering CMake forbids POST_BUILD hooks from a different directory scope." },
  { category: "signing", question: "Why did the initial CMake integration fail, and how was it fixed?", answer: "add_custom_command(TARGET ... POST_BUILD) failed with 'TARGET was not created in this directory' because can-utils creates its targets in its own subdirectory scope. The fix used an OUTPUT-based custom command plus a same-scope custom target that merely depends on the vendor target, which CMake allows across directories." },
  { category: "signing", question: "How does the Bazel signed_binary macro work?", answer: "It wraps native cc_binary() to build the binary, then adds a genrule() that runs sign_artifact.py against the binary's output and any key/cert/chain labels, producing a <name>.sig as a declared Bazel output \u2014 mirroring the same CLI contract used by CMake and Yocto." },
  { category: "signing", question: "Why is there exactly one shared sign_artifact.py instead of per-build-system logic?", answer: "Centralizing signing logic means CMake, Bazel, and Yocto all produce identical envelope semantics and get security fixes in one place; only the invocation glue differs per build system." },
  { category: "signing", question: "What happens if you request PKI signing but only supply a private key?", answer: "sign_artifact.py raises SigningError('PKI signing requires --cert and --chain') and exits non-zero \u2014 mode-specific required arguments are validated before any cryptographic operation runs." },
  { category: "signing", question: "How does the signer confirm the private key actually matches the certificate in PKI mode?", answer: "ensure_key_matches_certificate() extracts the public key from both the private key (openssl pkey -pubout) and the certificate (openssl x509 -pubkey -noout) and byte-compares them, raising SigningError on mismatch \u2014 covered by test_rejects_untrusted_pki_root_and_mismatched_leaf_key." },
  { category: "pki", question: "Why a 3-tier Root CA / Intermediate CA / Leaf hierarchy instead of one self-signed key?", answer: "It enables leaf-key rotation on a normal release cadence without ever touching deployed devices' trust anchor; only the long-lived, offline Root CA needs to be pinned on target." },
  { category: "pki", question: "Why does the verifier pin the Root CA instead of the leaf certificate?", answer: "Pinning the leaf would require re-provisioning every device on every key rotation. Pinning the Root CA lets openssl verify -CAfile root.pem validate any leaf issued by that root, so rotation never touches deployed trust material." },
  { category: "pki", question: "What does verify_artifact.py check for PKI mode, in order?", answer: "(1) envelope schema/fields are well-formed, (2) SHA-256 of the artifact matches artifact_sha256, (3) the certificate chain validates to the caller-supplied --root-ca, (4) the EC signature verifies against the leaf certificate's public key." },
  { category: "pki", question: "Why must --public-key and --root-ca be mutually exclusive CLI flags?", answer: "Mixing them would create ambiguity about which trust model is enforced. verify_artifact.py requires exactly one, matching the envelope's method field, so no code path silently falls back to a weaker check." },
  { category: "pki", question: "What real attack does the 'untrusted Root CA' adversarial test simulate?", answer: "A leaf certificate issued by a completely different, attacker-controlled CA hierarchy. The test builds a second full Root/Intermediate/Leaf chain and confirms openssl verify rejects the impostor leaf even though its own chain is internally valid." },
  { category: "pki", question: "This repo doesn't implement certificate revocation (CRL/OCSP) \u2014 why call that out?", answer: "It's an honest, real limitation: a compromised leaf key can be rotated, but an already-deployed device has no live channel to learn 'reject this specific leaf' before its stated validity expires. Surfacing gaps like this is more valuable than hiding them." },
  { category: "jtag", question: "How does the JTAG challenge-response protocol work end to end?", answer: "The target starts locked and issues a random 32-byte nonce. The host signs exactly those bytes with the same signing key material used for artifacts (jtag_authenticator.py). The target verifies the signature against its embedded trust anchor and unlocks exactly one debug session if valid." },
  { category: "jtag", question: "Why is the nonce single-use, and how is replay actually prevented?", answer: "DebugAuthenticationDevice clears its pending nonce to None immediately after any unlock decision, so a second submit_response() call with the same signed response has nothing to check against and returns False \u2014 proven by test_valid_response_unlocks_once_and_replay_fails." },
  { category: "jtag", question: "Why does JTAG authentication logic conceptually live at the boot-ROM/secure-enclave layer, not Linux userspace?", answer: "A userspace-only gate would be bypassable by replacing the OS. Anchoring it in boot ROM/secure firmware gives the JTAG gate the same trust root as the earliest, most-verified code on the chip." },
  { category: "jtag", question: "What's the difference between jtag_device_simulator.py and jtag/device_auth_stub/jtag_auth.c?", answer: "The Python simulator is a fast, testable stand-in used in CI/unit tests. The C stub models the firmware-level API shape (jtag_auth_issue_challenge/jtag_auth_submit_response) that would run in boot ROM/secure firmware on real silicon \u2014 explicitly documented as a reference model, not production hardware logic." },
  { category: "jtag", question: "How would this integrate with real OpenOCD tooling?", answer: "jtag/openocd/secure_unlock.cfg sources a target-specific config and calls jtag_authenticator.py to sign the target's nonce before issuing 'reset halt', so a debug host without the trusted key can't get past that pre-init hook." },
  { category: "jtag", question: "Why reuse the artifact-signing key material for JTAG instead of a separate JTAG-only key?", answer: "The original requirement was secure JTAG access using the same keys \u2014 so a device only trusts debug hosts holding the same root of trust as the software supply chain, instead of managing a second, independent key/PKI just for debug access." },
  { category: "buildsystems", question: "Why were four different real applications chosen instead of one toy binary?", answer: "To exercise four artifact classes a real automotive Yocto build actually produces: CLI tools (can-utils), a long-running daemon (Mosquitto), a bootloader with native FIT signing (U-Boot), and an out-of-tree kernel module (WireGuard) \u2014 each stresses the signing pipeline differently." },
  { category: "buildsystems", question: "Why are the upstream projects Git submodules instead of vendored/copied source?", answer: "Submodules pin an exact upstream commit SHA, keep the vendor tree byte-for-byte unmodified (important since signing must verify real, unaltered upstream builds), and let `git submodule update` cleanly pull upstream fixes." },
  { category: "buildsystems", question: "Why is signing added via parent-level build files instead of patching each submodule's own CMakeLists.txt?", answer: "Patching vendor build files creates ongoing merge pain against upstream and risks shipping a modified fork of a public security-relevant project. Parent-owned build glue keeps upstream cleanly updatable." },
  { category: "buildsystems", question: "What's the shared bbclass pattern in the Yocto layers, and why?", answer: "signing.bbclass centralizes SIGNING_METHOD/SIGNING_PRIVATE_KEY/SIGNING_CERT/SIGNING_CHAIN and a secure_sign_file() shell function; three application recipes inherit it and just call secure_sign_file() instead of re-implementing argument plumbing three times." },
  { category: "buildsystems", question: "Why does the Yocto image only ship public keys/certs, never private keys?", answer: "signing-keys.bb installs only a public Root CA cert to /etc/secure-signing/ on target. A private key inside a deployed image would let anyone who extracts the rootfs sign arbitrary artifacts as if they were the vendor." },
  { category: "supplychain", question: "What does the CycloneDX SBOM in this repo actually contain?", answer: "A root 'secure-signing-pipeline' component plus one component per submodule, each with a pkg:github/<owner>/<repo>@<commit-sha> PURL derived live from `git submodule status`, with an explicit dependency graph registered via bom.register_dependency()." },
  { category: "supplychain", question: "Why is the SBOM generated with cyclonedx-python-lib instead of hand-written JSON?", answer: "Using the standard library guarantees schema-valid CycloneDX 1.5 output that real downstream tooling (OSV-Scanner, dependency-track, etc.) can actually parse, rather than an ad-hoc JSON shape that only looks similar." },
  { category: "supplychain", question: "Why does scanning the SBOM with OSV-Scanner report 'No package sources found'?", answer: "pkg:github/... PURLs pinned by commit SHA represent a vendored source snapshot, not a published package + semver range. Ecosystem SCA tools key advisories by package name and version range, so a raw commit hash matches nothing." },
  { category: "supplychain", question: "What real vulnerability did this repo's own SCA scan find, and how was it fixed?", answer: "OSV-Scanner found idna 3.9.0 (PYSEC-2026-215, CVSS 6.9) and lxml 5.4.0 (PYSEC-2026-87, CVSS 7.5 XXE) as transitive dependencies of cyclonedx-bom. Both were fixed with explicit idna>=3.15 / lxml>=6.1.0 constraints, then re-scanned clean." },
  { category: "supplychain", question: "Why did the resolver pick vulnerable transitive versions when a patched version was already installed locally?", answer: "requirements.txt only pinned the two top-level packages. Without a full lockfile, a resolver (or a scan on a different machine/day) is free to pick the oldest version satisfying the loose constraint, even if something newer happens to be installed right now." },
  { category: "supplychain", question: "What does the release gate actually check, and what's its exit-code contract?", answer: "For each --artifact path, it requires a same-named .sig file that verifies via verify_artifact.py; any missing/failing signature adds a failure and exits 1 ('RELEASE BLOCKED'). Only if every artifact passes does it print 'RELEASE APPROVED' and exit 0." },
  { category: "supplychain", question: "Why does the release gate only warn (not block) when an artifact isn't mentioned in the SBOM?", answer: "The SBOM's job is inventory/traceability, not signing enforcement. Treating a naming mismatch as a hard failure would conflate two different controls, so the gate's sole blocking condition stays the signature check." },
  { category: "supplychain", question: "Why is the release gate fuzz-tested by importing evaluate_release() directly instead of shelling out per case?", answer: "Running 300 scenarios via subprocess-per-case would be far slower. Importing the function directly (while it still shells out to openssl/verify_artifact.py internally) keeps the fuzz loop fast enough to run on every CI build." },
  { category: "supplychain", question: "What's the core lesson behind the 'never ship unsigned binaries' checklist?", answer: "Publishing an SBOM proves what's inside a release, not that it's trustworthy. An SBOM can be generated and shared perfectly correctly while the binaries it describes are unsigned, wrongly re-signed, or tampered with \u2014 both controls must run, automated, every release." },
  { category: "secops", question: "Why is Bandit's severity threshold set to medium instead of failing on any finding?", answer: "The Python test helpers legitimately use subprocess.run() with argument lists (not shell=True) to call openssl, which Bandit correctly flags as Low-severity \u2014 not exploitable here. The threshold avoids false-positive CI failures while still catching Medium+." },
  { category: "secops", question: "Why is cppcheck only run against jtag/device_auth_stub/jtag_auth.c and not the vendored submodules?", answer: "This repo's SAST posture is 'scan code we own and can fix'. Running static analysis against upstream source would produce findings this repo has no authority to remediate; those belong upstream." },
  { category: "secops", question: "What does the DAST fuzz harness for verify_artifact.py actually assert?", answer: "Across hundreds of randomized malformed artifacts/envelopes/trust-material paths, the real verify_artifact.py subprocess must always exit 0 or 1 and never crash with an unhandled traceback or hang \u2014 proving it fails closed under adversarial input." },
  { category: "secops", question: "What's the philosophy behind the pentest/run_security_checks.sh authorized test lane?", answer: "It's scoped explicitly to this repo's own trust boundaries using only temporary keys and local files \u2014 it never discovers, scans, or interacts with external hosts/networks/devices, keeping it safe and repeatable rather than an offensive tool." },
  { category: "boot", question: "In the Boot Chain model, what's the difference between the secure core (HSM role) and the crypto engine?", answer: "The secure core is the policy and key-custody engine \u2014 it decides what may be verified/decrypted and owns root keys \u2014 while the crypto engine is a separate hardware accelerator (e.g. TI SA2UL-class) that executes the AES/SHA/ECC math on its behalf, holding no long-term keys itself." },
  { category: "boot", question: "Why is OTP/eFuse modeled as its own boot stage instead of a detail under Boot ROM?", answer: "OTP is physically un-updatable non-volatile memory and the true root-of-trust anchor \u2014 everything from Boot ROM onward is only as trustworthy as this fuse data, so it deserves its own explicit stage." },
  { category: "boot", question: "Why is the Boot Chain explicitly labeled 'inspired by TI TDA4x-class (Jacinto 7)' rather than presented as exact?", answer: "No web-browsing tool was available to verify the referenced external site or current TI documentation, so the model is deliberately hedged as illustrative of the general industry pattern, not a claim of verbatim proprietary accuracy." },
  { category: "gui", question: "Why does the documentation portal use localStorage instead of a backend for progress tracking?", answer: "The design goal was 'nothing ever leaves this page' \u2014 a static, dependency-free site with client-side-only state stays safely publishable via GitHub Pages with zero server, zero telemetry, and zero data-handling surface to secure." },
  { category: "gui", question: "Why was a 'mission/game' framing chosen for the documentation instead of plain prose docs?", answer: "It was requested to feel like a hands-on security range. Gamified objectives push a reader to actually exercise the same interactions the underlying Python tools perform, reinforcing concepts instead of just describing them." },
  { category: "process", question: "What's the single validation habit repeated across almost every change in this project?", answer: "Never claim something works without actually running it \u2014 real SBOM generation, real signer/verifier CLI calls, real OSV-Scanner runs, real test suite runs after every change \u2014 so every claim is backed by an actual terminal result, not an assumption." },
  { category: "boot", question: "What is TF-A (Trusted Firmware-A), and where does it fit in the boot chain modeled here?", answer: "TF-A is ARM's open-source reference implementation of Armv8-A secure-world firmware (stages BL1/BL2/BL31/BL32/BL33). On Jacinto-class SoCs it typically runs as BL31 on the main Cortex-A72 cluster \u2014 the EL3 secure monitor providing PSCI power-state services and SMC call routing between the non-secure world (U-Boot/Linux) and secure firmware. It's now modeled as its own stage between A72 SPL and U-Boot (BL33)." },
  { category: "boot", question: "What is TF-M, and is it the same thing as TIFS?", answer: "No. TF-M (Trusted Firmware-M) is ARM's separate, PSA-Certified open-source reference firmware for Armv8-M Cortex-M cores \u2014 a generic secure-partition runtime for constrained microcontrollers. TIFS is TI's own SoC-specific security firmware (evolved from the K3 architecture's DMSC); it plays a similar architectural role to what TF-M provides elsewhere, but TIFS is not built on or derived from TF-M \u2014 they are different projects that just share the 'Trusted Firmware' name." },
  { category: "boot", question: "What does TIFS actually do, in more detail, on a TI Jacinto-class SoC?", answer: "TIFS (TI Foundational Security), the evolution of the K3 architecture's DMSC (Device Manager and Security Controller), runs on a dedicated Cortex-M-class security core. It authenticates and unlocks the R5/A72 boot images, owns and never exposes root key material, brokers power/clock/resource management for the rest of the SoC, and communicates with other cores over a dedicated secure hardware messaging path (TI's Secure Proxy) rather than shared memory." }
];

const ARCHITECTURE_DECISIONS = [
  { category: "signing", title: "Detached signature envelope over embedded signing", decision: "Store signatures as a separate JSON .sig file adjacent to the artifact rather than embedding them in the binary.", rationale: "Keeps artifact bytes identical to the unsigned build (verifiable, portable across ELF/kernel-module/data artifacts) without needing a container format.", consequence: "Two files must always travel together; release tooling must never publish one without the other (this is exactly what motivated the release gate)." },
  { category: "signing", title: "Versioned envelope format string", decision: "Every envelope embeds \"format\": \"secure-signing-envelope/v1\".", rationale: "Lets the verifier immediately reject unknown/future formats instead of guessing field meanings, and allows a v2 format later without breaking v1 verification.", consequence: "Any future schema change must bump the version, and verify_artifact.py must explicitly support both during migration." },
  { category: "crypto", title: "SHA-256 as the sole digest algorithm", decision: "Hardcode digest_algorithm: sha256 rather than making it configurable.", rationale: "Avoids downgrade attacks where an attacker crafts an envelope claiming a weaker hash; matches the repo's explicit 'no MD5/SHA-1' rule.", consequence: "Migrating to SHA-3/BLAKE later requires a new format version, not a field toggle." },
  { category: "crypto", title: "EC P-256 as the default signing curve", decision: "Use prime256v1 EC keys by default across every key-generation script instead of RSA.", rationale: "Equivalent security to RSA-3072+ with much smaller keys/signatures and faster CI.", consequence: "Any future HSM/secure-element integration must support P-256 EC operations, not just RSA." },
  { category: "pki", title: "Three-tier CA hierarchy (Root/Intermediate/Leaf)", decision: "gen_ca_hierarchy.sh always produces all three tiers, never a flat root-signs-leaf design.", rationale: "Enables leaf-key rotation without ever touching deployed root trust.", consequence: "Verification requires chain validation logic (openssl verify -untrusted), not just a single signature check." },
  { category: "pki", title: "Pin Root CA on target, not the leaf certificate", decision: "verify_artifact.py's --root-ca flag expects the Root CA only; the leaf/chain travel inside the envelope.", rationale: "Devices never need reprovisioning when the signing leaf rotates.", consequence: "Compromise of the Root CA itself is catastrophic and unrecoverable without physical device access \u2014 its offline handling is critical." },
  { category: "pki", title: "Mutually exclusive --public-key / --root-ca flags", decision: "verify_artifact.py raises an error if both or neither trust flags are given.", rationale: "Prevents ambiguous or accidentally-weakened verification paths.", consequence: "Callers (CI, release gate) must always know exactly which trust mode they're operating in." },
  { category: "signing", title: "One shared sign_artifact.py/verify_artifact.py for every build system", decision: "CMake, Bazel, and Yocto all shell out to the same two scripts rather than each re-implementing crypto calls.", rationale: "A single place to patch bugs/CVEs in the signing logic itself.", consequence: "All three integrations depend on Python plus the repo's file layout being present at build time." },
  { category: "signing", title: "Shell out to system openssl instead of a Python crypto library", decision: "sign_artifact.py/verify_artifact.py/encrypt_artifact.py all invoke the openssl CLI via subprocess.", rationale: "Zero extra Python dependencies inside constrained Yocto/embedded build environments where system openssl is already guaranteed present.", consequence: "Behavior is coupled to the installed openssl version's CLI flags/output \u2014 discovered firsthand that `enc` lacks AEAD support." },
  { category: "signing", title: "Parent-owned CMake signing targets, not POST_BUILD hooks on vendor targets", decision: "Signing.cmake creates its own add_custom_command(OUTPUT ...) depending on the vendor's $<TARGET_FILE:...> instead of hooking the vendor target directly.", rationale: "CMake forbids add_custom_command(TARGET ...) from a different directory scope than where the target was created \u2014 discovered via a real build failure.", consequence: "Signature output paths must be explicitly wired to the vendor's actual binary directory rather than assumed." },
  { category: "buildsystems", title: "Real upstream submodules instead of toy demo binaries", decision: "can-utils, mosquitto, u-boot, and wireguard-linux-compat are pulled in as genuine, unmodified Git submodules.", rationale: "Reflects a real automotive Yocto build and exercises CLI/daemon/bootloader/kernel-module artifact classes distinctly.", consequence: "Repo size and clone time grow, and any submodule update must be re-validated against the signing pipeline." },
  { category: "buildsystems", title: "Never patch vendor build files directly", decision: "All signing integration lives in parent-level CMakeLists.txt / BUILD.bazel / Yocto recipes, never inside third_party/*.", rationale: "Keeps submodules cleanly updatable via `git submodule update` with zero merge conflicts.", consequence: "Some integration needs extra plumbing (explicit binary directory paths) instead of simpler in-tree hooks." },
  { category: "buildsystems", title: "Shared Yocto signing.bbclass instead of per-recipe crypto calls", decision: "meta-secure-signing/classes/signing.bbclass centralizes signing configuration behind one secure_sign_file() function.", rationale: "Three application recipes need identical signing behavior; duplicating it three times risks drift.", consequence: "Any recipe wanting custom signing behavior must extend the shared class rather than diverging locally." },
  { category: "buildsystems", title: "Ship public trust material in the image, never private keys", decision: "signing-keys.bb only ever installs a Root CA public certificate to /etc/secure-signing/.", rationale: "A private key inside a deployed image is a total compromise the moment the rootfs is extracted.", consequence: "Private keys must be supplied out-of-band at build time via protected CI configuration/HSM, never baked into any recipe output." },
  { category: "jtag", title: "JTAG unlock reuses the artifact-signing key material", decision: "jtag_authenticator.py calls the same sign_artifact.py crypto path used for build artifacts.", rationale: "A single root of trust for both software authenticity and debug access, per the original requirement.", consequence: "Compromising the signing key compromises the debug gate too \u2014 key protection now carries higher stakes." },
  { category: "jtag", title: "Nonce is single-use by construction", decision: "DebugAuthenticationDevice clears its pending nonce immediately after any unlock decision, success or failure.", rationale: "Prevents replaying a previously valid signed response to reopen a session later.", consequence: "Every debug session requires a fresh round-trip; there is no 'stay unlocked' mode by design." },
  { category: "jtag", title: "JTAG trust logic modeled at the boot-ROM/secure-enclave boundary, not Linux userspace", decision: "jtag/device_auth_stub/jtag_auth.c is documented as a boot-ROM/secure-firmware reference, explicitly not production hardware logic.", rationale: "A userspace-only gate would be bypassable by replacing the OS; real debug authentication must live below the OS.", consequence: "The repo's C stub cannot be used as-is on real silicon \u2014 it is an API-shape reference only." },
  { category: "jtag", title: "Separate Python simulator from the C hardware stub", decision: "jtag_device_simulator.py (testable, fast) and jtag_auth.c (hardware API reference) are two different artifacts, not one.", rationale: "Unit/fuzz testing needs a fast Python model; hardware engineers need a C API shape matching real firmware conventions.", consequence: "Two implementations of the same protocol must be kept conceptually in sync manually." },
  { category: "supplychain", title: "Generate SBOM via cyclonedx-python-lib, not hand-written JSON", decision: "generate_sbom.py builds a Bom object programmatically instead of constructing JSON by hand.", rationale: "Guarantees schema-valid CycloneDX 1.5 output that real downstream SBOM tooling can actually parse.", consequence: "The SBOM generator now depends on a pinned third-party library version that itself needed SCA scanning." },
  { category: "supplychain", title: "SBOM component version equals the live submodule commit SHA", decision: "generate_sbom.py reads `git submodule status` at generation time rather than hardcoding versions.", rationale: "An SBOM only has value if it reflects exactly what's currently checked out, not a stale snapshot.", consequence: "The SBOM must be regenerated before every scan/release; a cached SBOM can silently go stale." },
  { category: "supplychain", title: "Explicit dependency-graph registration in the SBOM", decision: "Call bom.register_dependency() for each submodule instead of leaving the graph implicit.", rationale: "cyclonedx-python-lib warns of an incomplete dependency graph otherwise, and a complete graph is required by consumers reasoning about transitive impact.", consequence: "Adding a new tracked component requires remembering to register its dependency edge too." },
  { category: "supplychain", title: "Run SCA against the SBOM even though it returns no results", decision: "run_sca.sh deliberately scans sbom.json and prints the 'No package sources found' outcome rather than skipping that step.", rationale: "Hiding a known tooling gap would be dishonest and let someone assume submodule CVEs are covered when they aren't.", consequence: "The script's own output explicitly documents this as an intentional, visible limitation, not a bug to silently fix." },
  { category: "supplychain", title: "Pin the OSV-Scanner version in CI, don't fetch 'latest'", decision: "run_sca.sh downloads a specific tagged release (v2.5.1), not a moving /latest/ URL.", rationale: "Reproducible CI runs \u2014 a moving 'latest' binary could change scan behavior without any change to this repo's own code.", consequence: "The pinned version must be manually bumped periodically to pick up scanner/database improvements." },
  { category: "supplychain", title: "Add minimum-version constraints, not a full frozen lockfile, after the CVE finding", decision: "requirements.txt adds idna>=3.15 / lxml>=6.1.0 rather than pip-freeze-style exact pins for every transitive package.", rationale: "Minimum-safe-version constraints fix the specific known vulnerabilities while still letting pip resolve compatible newer releases automatically.", consequence: "Future vulnerabilities in other transitive packages could still slip in silently between scans \u2014 called out as a real gap." },
  { category: "supplychain", title: "Release gate is a separate tool from verify_artifact.py", decision: "check_release_gate.py orchestrates calling verify_artifact.py per artifact rather than folding gate logic into the verifier.", rationale: "Verification (single artifact) and release-readiness (many artifacts, pass/fail summary, SBOM cross-reference) are different responsibilities.", consequence: "Two CLIs must be composed together in CI/release scripts instead of one command doing everything." },
  { category: "supplychain", title: "SBOM-name mismatch is a warning, not a blocking failure, in the release gate", decision: "check_sbom_covers_artifacts() only prints WARN lines; only missing/invalid signatures set the exit code to 1.", rationale: "Keeps the gate's single, unambiguous blocking condition as 'is it signed and verified', avoiding confusion with an actual trust failure.", consequence: "A real SBOM/artifact naming mismatch could go unnoticed if nobody reads the WARN output \u2014 a deliberate scope trade-off." },
  { category: "secops", title: "evaluate_release() is importable, not subprocess-only", decision: "check_release_gate.py exposes a pure function that the fuzz harness imports directly.", rationale: "Lets fuzz_release_gate.py run hundreds of scenarios fast without spawning a new Python interpreter per case.", consequence: "The CLI wrapper and the importable core must be kept behaviorally identical \u2014 a real coupling risk if one is edited alone." },
  { category: "secops", title: "Bandit and cppcheck fail only at medium+ severity", decision: "run_sast.sh passes --severity-level medium to Bandit rather than treating every finding as fatal.", rationale: "Legitimate Low-severity subprocess-usage findings exist in test helpers calling openssl with argument lists (not shell=True) \u2014 real code, not a vulnerability.", consequence: "A genuinely Low-severity but real issue could be missed unless someone reviews the informational output too." },
  { category: "secops", title: "cppcheck scope is limited to repository-owned C code", decision: "run_sast.sh only scans jtag/device_auth_stub/jtag_auth.c, not the vendored submodules.", rationale: "This repo has no authority to fix findings in upstream can-utils/mosquitto/u-boot/wireguard source; those belong upstream.", consequence: "Vendored C code security posture depends entirely on upstream projects' own practices." },
  { category: "secops", title: "DAST fuzzes the process boundary, not internal functions", decision: "fuzz_verify_artifact.py spawns verify_artifact.py as a real subprocess with malformed CLI input rather than importing functions directly.", rationale: "The real production security boundary is 'an external process is handed untrusted files' \u2014 testing exactly there catches crashes/hangs a unit test might miss.", consequence: "Slower test execution than in-process fuzzing, an accepted trade-off for boundary fidelity." },
  { category: "secops", title: "Fail-closed is an explicit, tested requirement everywhere", decision: "Every verifier/gate component's tests assert failure on ambiguous or malformed input, never silent success.", rationale: "A cryptographic verification system that fails open on unexpected input is a security bug waiting to be exploited.", consequence: "New code paths must always be reasoned about from 'what happens on garbage input' first." },
  { category: "secops", title: "The authorized pentest lane is scoped to this repo's own trust boundaries only", decision: "pentest/run_security_checks.sh never targets external hosts/networks/devices \u2014 only local temporary keys and files.", rationale: "Keeps the tooling unambiguously safe to run anywhere without accidentally becoming an offensive scanning tool.", consequence: "It cannot double as a general-purpose penetration-testing toolkit; it is narrowly a repeatable local regression suite." },
  { category: "crypto", title: "AES-256-CBC + HMAC-SHA256 instead of AES-GCM", decision: "encrypt_artifact.py/decrypt_artifact.py implement Encrypt-then-MAC manually rather than using an AEAD cipher mode.", rationale: "Confirmed openssl's enc CLI rejects AEAD ciphers outright; EtM achieves equivalent guarantees using only working CLI primitives.", consequence: "Two separate primitives (cipher + MAC) must both be implemented correctly and composed in the right order." },
  { category: "crypto", title: "Single 64-byte key file split into two 32-byte keys", decision: "gen_confidentiality_key.sh generates one 64-byte random file; the first half is the AES key, the second half is the HMAC key.", rationale: "Independent keys for encryption and authentication is EtM best practice; a single file is simpler to provision/rotate than two.", consequence: "Any key-management tooling must know the fixed 64-byte layout convention rather than treating it as an opaque blob." },
  { category: "crypto", title: "Verify HMAC before attempting decryption", decision: "decrypt_artifact.py computes and compares the expected HMAC (constant-time) before calling openssl enc -d.", rationale: "Refuses to run AES decryption on attacker-controlled, unauthenticated ciphertext at all, eliminating a whole class of oracle attacks.", consequence: "A single bit-flip anywhere produces a clean, generic 'HMAC verification failed' error with zero information leaked about what was wrong." },
  { category: "crypto", title: "Recommend encrypt-then-sign, not sign-then-encrypt", decision: "README documents encrypting an artifact first, then signing the resulting .enc envelope with sign_artifact.py.", rationale: "The EC signature then authenticates the exact ciphertext bytes that ship, so post-signing tampering with the envelope is still caught.", consequence: "Verifiers must check the signature over the .enc file, then separately decrypt+HMAC-check \u2014 two sequential trust operations." },
  { category: "boot", title: "Separate OTP/eFuse from Boot ROM as distinct concepts", decision: "The Boot Chain model adds an explicit 'OTP / eFuse' stage before 'Boot ROM' rather than treating fuse data as a ROM detail.", rationale: "OTP is the true, physically-immutable root-of-trust anchor; conflating it with the code that reads it obscured which component can't be attacked.", consequence: "One more stage to walk through, but an accurate separation of immutable data from immutable code." },
  { category: "boot", title: "Separate secure core (HSM role) from crypto engine as distinct stages", decision: "Added a dedicated 'Crypto engine' stage distinct from 'Secure core (HSM role)'.", rationale: "In real SoCs these are genuinely different hardware blocks \u2014 one owns keys and decides policy, the other executes the crypto math \u2014 and conflating them misrepresented the architecture.", consequence: "Readers now see explicitly that 'decides trust' and 'executes crypto' are not the same component, which matters for threat modeling." },
  { category: "boot", title: "Explicitly hedge the Boot Chain as 'inspired by,' not a verbatim reproduction", decision: "Every mention of the TI TDA4x/Jacinto 7 reference includes language clarifying it's illustrative, not proprietary-accurate.", rationale: "No web-browsing tool was available to verify the referenced external site or pull current TI documentation, so overclaiming precision risked misinformation.", consequence: "The model stays educationally useful and directionally accurate without asserting authority it can't back up." },
  { category: "gui", title: "Static, dependency-free portal (no framework, no build step)", decision: "docs/portal is plain HTML/CSS/vanilla JS with zero npm packages or bundlers.", rationale: "Zero install friction, trivially publishable via GitHub Pages, and keeps the 'nothing leaves this page' guarantee simple to audit.", consequence: "All interactivity is hand-rolled rather than using a component library, trading development speed for simplicity and auditability." },
  { category: "gui", title: "Progress state lives only in localStorage, never a backend", decision: "Trust score/objectives persist via a single namespaced localStorage key, with no server or telemetry endpoint at all.", rationale: "Consistent with the repo's broader 'nothing leaves this page' security posture, with zero data-handling surface to secure.", consequence: "Progress is per-browser and per-device; there is no cross-device sync or usage analytics \u2014 an accepted trade-off." },
  { category: "gui", title: "Mission/game framing instead of static prose documentation", decision: "The portal is organized as numbered missions with objectives, XP, and a live console log, rather than a conventional docs page.", rationale: "Gamified objectives push a reader to actually perform the same interactions the underlying Python tools perform instead of passively reading.", consequence: "Content had to be restructured around discrete, clickable actions rather than long-form prose." },
  { category: "gui", title: "Reuse existing visual components for new content", decision: "The AES layout and Supply Chain panels reuse the same field-row CSS classes built for the signing envelope comparison.", rationale: "Keeps visual language consistent across missions and avoids CSS sprawl as more missions were added incrementally.", consequence: "New content is somewhat constrained to fit the existing card/field visual grammar rather than getting fully bespoke layouts." },
  { category: "process", title: "Validate every change by actually running it, not just reading the code", decision: "Every meaningful change was followed by a real terminal execution \u2014 real openssl commands, real builds, real scanner runs, real test suite runs \u2014 before being reported as done.", rationale: "A security-focused reference repo has little value if its claims aren't independently verifiable.", consequence: "Slower iteration per turn, in exchange for a much higher-confidence, defensible final state." },
  { category: "process", title: "Prefer multiple small, focused CI workflows over one monolith", decision: "secure-pipeline.yml, security-scans.yml, and deploy-docs.yml are three separate workflows rather than one giant pipeline.", rationale: "Each has a different trigger/scope/blast-radius \u2014 a docs-only change shouldn't rebuild can-utils across the full signing matrix.", consequence: "Slightly more workflow files to maintain, in exchange for faster, more targeted CI runs and clearer failure attribution." },
  { category: "process", title: "Least-privilege GitHub Actions permissions for Pages deployment", decision: "deploy-docs.yml requests only contents: read, pages: write, id-token: write \u2014 nothing broader.", rationale: "The deployment job only ever needs to read the repo and publish to Pages; broader access would be unnecessary blast radius.", consequence: "Any future workflow step needing additional GitHub API access must explicitly request it, forcing a conscious review each time." },
  { category: "process", title: "Broad .gitignore key/cert/signature patterns with a narrow allowlist", decision: ".gitignore blocks *.key, *.pem, *.sig, *.csr, *.srl globally, with one explicit exception for a legitimate committed public-cert path.", rationale: "Defaults to 'don't commit sensitive-looking crypto material' rather than trusting every contributor to remember not to `git add` a generated key.", consequence: "Legitimate new public-cert files must remember their own explicit allowlist exception, or they're silently ignored." },
  { category: "process", title: "Explicit deliverable CHECKLIST.md, despite a general no-unsolicited-markdown rule", decision: "security/release-gate/CHECKLIST.md was created as a real file even though the standing instruction is to avoid unsolicited markdown docs.", rationale: "The user explicitly asked to 'show me the checklist' as a named deliverable \u2014 a requested artifact, not incidental documentation.", consequence: "This distinction (requested deliverable vs. unsolicited change-log doc) had to be actively reasoned about, not applied mechanically." },
  { category: "process", title: "Real submodule pins over synthetic/mocked upstream data everywhere", decision: "SBOM commits, boot-chain references, and CVE case studies all use genuine data from real submodules, real OSV.dev advisories, and real tool output.", rationale: "A security case-study repo loses credibility the moment its 'evidence' is made up rather than reproducible.", consequence: "Static GUI reference data (e.g. SBOM commit hashes shown in the console) will drift if submodules update without regenerating the SBOM." },
  { category: "process", title: "Treat this as an accumulating, backward-compatible system rather than one-off asks", decision: "Every new feature (SBOM, SCA, release gate, AES, boot chain, interview bank) was wired into the same envelope contract, test conventions, CI structure, and GUI mission pattern already established.", rationale: "Consistency compounds \u2014 future readers can predict where a given concern lives and how it's tested without re-learning a new pattern per feature.", consequence: "Some individually-simpler implementations were passed over in favor of ones that fit established conventions." },
  { category: "boot", title: "Add TF-A (BL31) as its own boot-chain stage", decision: "Insert an explicit 'TF-A (BL31)' stage between 'A72 SPL' and 'U-Boot' in the Boot Chain reference model.", rationale: "TF-A is a real, industry-standard, separately-named component (ARM's own open-source secure firmware) actually present in TI Jacinto-class boot flows; folding its role into a generic 'bootloader stage' label was an accuracy gap once pointed out.", consequence: "The Boot Chain model has one more concrete, correctly-named stage to trace, and the U-Boot stage description now explicitly notes it is the ARM-terminology BL33 stage launched by TF-A." },
  { category: "boot", title: "Document TF-M as a distinct, non-TIFS concept rather than omitting it", decision: "Add an explicit callout distinguishing TF-M (Trusted Firmware-M, generic PSA/Cortex-M firmware) from TIFS (TI's own SoC-specific security firmware), instead of leaving TF-M unmentioned or implying TIFS is 'based on' it.", rationale: "The two are easy to conflate by name similarity ('Trusted Firmware-*') but are different projects with different scopes; stating this explicitly prevents a plausible-sounding but incorrect claim.", consequence: "The Boot Chain mission needed a short comparison panel rather than shoehorning TF-M into a sequence where it doesn't actually run." }
];
