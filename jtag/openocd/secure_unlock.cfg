# Run jtag_authenticator.py after obtaining a target nonce, before reset halt.
# Board-specific transport, TAP, nonce retrieval, and response delivery belong in target_stub.cfg.
source [find target_stub.cfg]